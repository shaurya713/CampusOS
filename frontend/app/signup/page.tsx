"use client";

import Link from "next/link";
import { ArrowRight, Orbit, ShieldCheck } from "lucide-react";
import { FormEvent, useState } from "react";
import { api } from "../../lib/api";

export default function Signup() {
  const [message, setMessage] = useState(""); const [error, setError] = useState("");
  async function submit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault(); setError(""); setMessage(""); const form = new FormData(event.currentTarget);
    if (form.get("password") !== form.get("confirm_password")) { setError("Passwords do not match."); return; }
    try { await api.post("/auth/register", { full_name: form.get("full_name"), email: form.get("email"), phone: form.get("phone") || null, password: form.get("password"), student_id: form.get("student_id"), department: form.get("department"), year: Number(form.get("year")), section: form.get("section") }); setMessage("Account created successfully. You can sign in now."); event.currentTarget.reset(); }
    catch (request: any) { setError(request?.response?.data?.error?.message || request?.response?.data?.detail || "Unable to create account. Please try again."); }
  }
  return <main className="auth-page"><div className="auth-frame"><aside className="auth-aside"><span className="brand-lockup"><i className="logo"><Orbit /></i>campus<span style={{ color: "#7dd3fc" }}>os</span></span><p className="eyebrow" style={{ color: "#8ab6f8", marginTop: 50 }}>STUDENT ACCESS</p><h1>Support for campus life, without the follow-up chase.</h1><p>Make your profile once. Track requests, comments, announcements, and updates from one place.</p><div className="auth-stat-grid"><div className="auth-stat"><b>1 workspace</b><span>Every campus request</span></div><div className="auth-stat"><ShieldCheck size={17} color="#7dd3fc" /><b>Private</b><span>Your issues stay yours</span></div></div></aside><section className="auth-main"><form className="auth-card" onSubmit={submit}><span className="brand-lockup"><i className="logo"><Orbit /></i>campus<span>os</span></span><h1>Create your account</h1><p className="subtle">Use your official student details to get started.</p><label className="input-label">FULL NAME</label><input name="full_name" required minLength={2} /><label className="input-label">COLLEGE EMAIL</label><input name="email" type="email" required /><div className="split"><div><label className="input-label">STUDENT ID</label><input name="student_id" required /></div><div><label className="input-label">DEPARTMENT</label><input name="department" placeholder="CSE" required /></div></div><div className="split"><div><label className="input-label">YEAR</label><select name="year" defaultValue="1"><option value="1">Year 1</option><option value="2">Year 2</option><option value="3">Year 3</option><option value="4">Year 4</option></select></div><div><label className="input-label">SECTION</label><input name="section" placeholder="A" required /></div></div><label className="input-label">PHONE NUMBER</label><input name="phone" inputMode="tel" /><label className="input-label">PASSWORD</label><input name="password" type="password" minLength={8} placeholder="Uppercase, lowercase, number" required /><label className="input-label">CONFIRM PASSWORD</label><input name="confirm_password" type="password" minLength={8} required />{error && <p role="alert" style={{ color: "var(--danger)" }}>{error}</p>}{message && <p role="status" style={{ color: "var(--success)" }}>{message}</p>}<button className="button" style={{ width: "100%" }}>Create account <ArrowRight size={16} /></button><p className="auth-footer">Already have an account? <Link href="/login">Sign in</Link></p></form></section></div></main>;
}
