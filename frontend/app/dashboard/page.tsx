"use client";
import {useEffect,useState} from "react";import {Plus, ArrowUpRight} from "lucide-react";import {api,setToken} from "../../lib/api";import {AppShell} from "../../components/app-shell";
type Item={id:string;reference_no:string;title:string;status:string;priority:string;created_at:string};
export default function Dashboard(){const [items,setItems]=useState<Item[]>([]);useEffect(()=>{const t=sessionStorage.getItem("campusos_access");if(!t){window.location.href="/login";return}setToken(t);api.get("/complaints?limit=5").then(r=>setItems(r.data.data))},[]);const active=items.filter(x=>!['resolved','closed','cancelled'].includes(x.status)).length;return <AppShell title="Good to see you"><div className="grid"><div className="card metric"><b>{items.length}</b><small>Recent complaints</small></div><div className="card metric"><b>{active}</b><small>Active issues</small></div><div className="card metric"><b>{items.filter(x=>x.status==='resolved').length}</b><small>Resolved</small></div><div className="card metric"><b>{items.filter(x=>x.priority==='critical').length}</b><small>Critical</small></div></div><div className="split" style={{marginTop:12}}><div className="card"><div className="row"><h3 style={{margin:0}}>Recent activity</h3><a className="subtle" href="/complaints">View all <ArrowUpRight size={14}/></a></div><table className="table"><thead><tr><th>ISSUE</th><th>STATUS</th><th>PRIORITY</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><b>{x.title}</b><br/><small className="subtle">{x.reference_no}</small></td><td><span className="badge">{x.status}</span></td><td>{x.priority}</td></tr>)}{!items.length&&<tr><td colSpan={3} className="empty">No activity yet.</td></tr>}</tbody></table></div><div className="card"><p className="eyebrow">QUICK ACTION</p><h2 style={{margin:'5px 0'}}>Need something fixed?</h2><p className="subtle">Report an issue with details and CampusOS will classify and route it.</p><a className="button" href="/complaints/new"><Plus size={16}/>Raise complaint</a></div></div></AppShell>}
