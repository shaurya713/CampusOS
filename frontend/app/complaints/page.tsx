"use client";
import {useEffect,useState} from "react";import {Plus,Search} from "lucide-react";import {api,setToken} from "../../lib/api";import {AppShell} from "../../components/app-shell";
type Item={id:string;reference_no:string;title:string;status:string;priority:string;created_at:string};
export default function Complaints(){const [items,setItems]=useState<Item[]>([]);const [search,setSearch]=useState('');useEffect(()=>{const t=sessionStorage.getItem("campusos_access");if(!t)return;setToken(t);api.get("/complaints").then(r=>setItems(r.data.data))},[]);const filtered=items.filter(x=>(x.title+x.reference_no).toLowerCase().includes(search.toLowerCase()));return <AppShell title="Complaints" action={<a className="button" href="/complaints/new"><Plus size={16}/>Raise issue</a>}><div className="card"><div className="row" style={{marginBottom:10}}><div><b>All issues</b><p className="subtle" style={{margin:2}}>Track every request and its current status.</p></div><div style={{position:'relative',width:230}}><Search size={16} style={{position:'absolute',left:9,top:10,color:'#667085'}}/><input className="search" style={{paddingLeft:31,margin:0}} placeholder="Search" value={search} onChange={e=>setSearch(e.target.value)}/></div></div><table className="table"><thead><tr><th>REFERENCE</th><th>ISSUE</th><th>STATUS</th><th>PRIORITY</th></tr></thead><tbody>{filtered.map(x=><tr key={x.id}><td className="subtle">{x.reference_no}</td><td><b>{x.title}</b><br/><small className="subtle">{new Date(x.created_at).toLocaleDateString()}</small></td><td><span className="badge">{x.status}</span></td><td>{x.priority}</td></tr>)}{!filtered.length&&<tr><td className="empty" colSpan={4}>No matching complaints.</td></tr>}</tbody></table></div></AppShell>}
