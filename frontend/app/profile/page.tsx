"use client";
import {ChangeEvent,FormEvent,useEffect,useState}from"react";import{api,setToken}from"../../lib/api";import{AppShell}from"../../components/app-shell";
export default function Profile(){const[me,setMe]=useState<any>();const[n,setN]=useState("");useEffect(()=>{const t=sessionStorage.getItem("campusos_access");if(!t){location.href="/login";return}setToken(t);api.get("/auth/me").then(r=>setMe(r.data.data))},[]);async function photo(e:ChangeEvent<HTMLInputElement>){const f=e.target.files?.[0];if(!f)return;if(f.size>2097152)return setN("Photo must be under 2 MB");const d=new FormData();d.append("file",f);const r=await api.post("/uploads/image",d);setMe({...me,profile_photo_url:r.data.data.url})}async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await api.patch("/auth/me",{full_name:f.get("name"),phone:f.get("phone"),permanent_address:f.get("address"),profile_photo_url:me.profile_photo_url});setMe(r.data.data);setN("Saved")};return <AppShell title="Profile & identity">{me&&<form className="card" style={{padding:22,maxWidth:600}} onSubmit={save}>{me.profile_photo_url?<img src={`http://localhost:8000${me.profile_photo_url}`} style={{width:70,height:70,borderRadius:18,objectFit:"cover"}}/>:<div className="avatar">{me.full_name.slice(0,2)}</div>}<label className="button ghost">Upload profile photo (max 2 MB)<input hidden type="file" accept="image/*" onChange={photo}/></label><label>NAME</label><input name="name" defaultValue={me.full_name}/><label>PHONE</label><input name="phone" defaultValue={me.phone}/><label>PERMANENT ADDRESS</label><textarea name="address" defaultValue={me.permanent_address}/><p className="subtle">Government ID is securely retained and not displayed.</p>{n&&<p>{n}</p>}<button className="button">Save profile</button></form>}</AppShell>}
